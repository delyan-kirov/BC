# this is an expression

pub a = 1

int b = let c
	= a + a
	in c + 2

int foo
	= \x = 2*x + b

int baz = foo 2

int baz = foo 5

int fib = \x =
	if x ?= 0 => 0
	else if x ?= 1 => 1
	else let previous = fib (x - 1)
		 in let preprevious = fib (x - 2)
		 in previous + preprevious

int fibtwelve = fib 15

pub powtwo = \x =
	if x ?= 0 => 1
	else 2 * (powtwo (x - 1))


int powtwofive = powtwo 50

