ext init_window: C_int -> C_int -> C_str = @DLL{C, "./bin/raylib.so"}

pub loopy =
	let fps = set_target_fps 10 in
	let window = init_window 800 400 "Hello world!" in
	while !window_should_close =>
		begin_drawing +
		(clear_background (rand 1)) +
		end_drawing
